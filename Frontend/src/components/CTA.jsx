import React from 'react';

const CTA = () => {
  return (
    <section className="cta-section">
      <div className="cta-content">
        <h2>Ready to reserve your table?</h2>
        <div className="cta-actions">
          <button className="btn-cta-primary">Book a Table Now</button>
          <button className="btn-cta-secondary">Browse Restaurants</button>
        </div>
      </div>
    </section>
  );
};

export default CTA;
